# Case review — sales_agent_followup_pipeline

- case: `docs/simulador/contrato_v0/casos/sales_agent_followup_pipeline_v1.yaml`
- variants: `docs/simulador/contrato_v0/variantes/sales_agent_followup_pipeline__aurora_primary_v1.yaml`, `docs/simulador/contrato_v0/variantes/sales_agent_followup_pipeline__nova_resim_v1.yaml`
- generated_at: 2026-05-23T22:52:03.114Z
- claude_status: completed

## Automatic checks

### case factory

status: PASS

```text
case factory OK
```

### case yaml

status: PASS

```text
case yaml quality OK (1 files)
```

## Local review findings

No local P0/P1/P2 findings.

## Claude prompt

```text
You are Claude Case Critic for Itera. READ-ONLY. Do not edit files.

Goal: review one simulator case as an adversarial product-quality reviewer.

Read these files before judging:
- docs/simulador/case_factory/CASE_REVIEW_PROTOCOL.md
- docs/simulador/case_factory/CASE_CREATION_SKILL.md
- docs/simulador/case_factory/CASE_HIG.md
- docs/simulador/case_factory/CASE_TAXONOMY.yaml
- docs/simulador/case_factory/EXERCISE_BLOCK_CATALOG.yaml
- docs/simulador/case_factory/CASE_RUBRIC_V1.md
- docs/simulador/case_factory/MANAGER_RESULTS_MODEL.md
- docs/simulador/case_factory/CASE_QUALITY_CHECKLIST.md
- docs/simulador/contrato_v0/casos/sales_agent_followup_pipeline_v1.yaml
- docs/simulador/contrato_v0/variantes/sales_agent_followup_pipeline__aurora_primary_v1.yaml
- docs/simulador/contrato_v0/variantes/sales_agent_followup_pipeline__nova_resim_v1.yaml

Evaluate:
1. Does this measure operational judgment, not knowledge?
2. Does the manager assignment brief clearly explain when to assign the case?
3. Are the exercises from the canonical catalog and appropriate for the level?
4. Does every step emit evidence for the six dimensions?
5. Are risk events observable and supported by transcript/action evidence?
6. Does it avoid spoilers, prefilled participant answers, and fake choices?
7. Does primary/resim measure transfer rather than memory?
8. Would these synthetic participants produce distinguishable reports?

Synthetic participant set:
- strong: minimizes data, uses approved exercise flow, validates output, catches high-risk actions, and writes a manager-ready decision with owner, metric, and next step.
- average: completes the flow but misses one medium issue such as: Incluye email, telefono o notas internas en el prompt..
- risky: optimizes speed or autonomy and triggers a high-risk failure such as: Permite envio externo sin approval gate..

Output format:
GLOBAL: PASS | FAIL | CONDITIONAL
P0: blockers
P1: must fix before publish
P2: polish/monitor
For each issue include: severity, file/section, why it matters, correction expected.
Then include: Manager 30-second test, Exercise fit verdict, Research freshness verdict, Final route to fix.
```

## Claude Case Critic

GLOBAL: CONDITIONAL

P0:
- (ninguno)

P1:
- **severity:** P1
  **file/section:** `sales_agent_followup_pipeline_v1.yaml` → `steps[0].fields` (step 1, data_table_triage)
  **why it matters:** Los campos PII tienen opciones pre-acotadas que excluyen el comportamiento riesgoso. `contact_name: [drop, hash]`, `email: [drop, hash]`, `internal_notes: [drop, summarize]` — no existe la opción `keep` aunque `data_policy.allowed_transformations` la incluye. El failure_mode "Incluye email, telefono o notas internas en el prompt" no puede dispararse aquí; queda solo disponible en step 2 (brief abierto). Esto vuelve a step 1 una "checklist de completion", no un punto de discriminación. El participante `average` definido por el spec del set sintético no puede equivocarse en datos en el momento en que el caso pide decisiones de datos.
  **correction expected:** Añadir `keep` como opción visible para `contact_name`, `email`, `internal_notes` y `competitor_mentioned`. La acción "incorrecta" debe estar disponible o no estamos midiendo juicio.

- **severity:** P1
  **file/section:** `sales_agent_followup_pipeline_v1.yaml` → `case_factory_meta.time_pressure.total_seconds` vs `steps[*].target_seconds`
  **why it matters:** Total timer = 1440 s. Suma de `target_seconds` (240+420+300+180+300+240) = 1680 s, 4 minutos por encima del límite. Si los `target_seconds` son la lectura interna de cuánto requiere cada paso, el caso está calibrado para que todos pasen a `overtime_seconds`. Si son holguras, hay que aclararlo. La aritmética actual hace ruidosas las métricas de presión temporal.
  **correction expected:** O subir `total_seconds` a ~1800 (30 min) y subir `duration_estimate_min`, o recortar `target_seconds` (por ejemplo bajar step 2 a 360 y step 3 a 240) para sumar ≤1440 con margen real.

P2:
- **severity:** P2
  **file/section:** `case_factory_meta.exercise_types`
  **why it matters:** Declara `workflow_builder` pero ningún `step.exercise_type` lo usa. Inconsistencia entre meta y steps; reduce confianza en el manifiesto.
  **correction expected:** Quitar `workflow_builder` del listado, o reemplazar uno de los dos `tradeoff_decision_memo` consecutivos (steps 4–6) por un paso de workflow real entre datos y permisos.

- **severity:** P2
  **file/section:** `EXERCISE_BLOCK_CATALOG.yaml` → bloque `data_table_triage` (`levels: [N1, N2]`) vs uso en caso N3
  **why it matters:** El catálogo restringe `data_table_triage` a N1/N2; el caso N3 lo usa como apertura. Es defendible —minimización siempre aplica— pero hoy el caso viola el catálogo y el reviewer automático puede levantar warning silencioso.
  **correction expected:** Ampliar `data_table_triage.levels` a `[N1, N2, N3]` en el catálogo (con nota: "en N3 cumple rol de minimización pre-agente"), o sustituir step 1 por bloque permitido en N3.

- **severity:** P2
  **file/section:** `manager_outcome.assignment_brief`
  **why it matters:** Es esencialmente verbatim el ejemplo de `MANAGER_RESULTS_MODEL.md`. Funciona, pero si éste es el golden case, copia textual del template degrada el valor pedagógico del propio template (managers verán el mismo párrafo en docs y en runtime).
  **correction expected:** Reescribir 1-2 frases con detalles específicos del caso (HubSpot/Gmail/SaaS LATAM, demo VP) para que no sea genérico.

- **severity:** P2
  **file/section:** `steps[1]` agent_brief_builder vs `followup`
  **why it matters:** El followup ("El modelo propone enviar emails automaticos si confidence > 0.8. Que cambias?") presenta el umbral 0.8 que reaparece literal en step 3 como `send_email` con confidence > 0.8 y en variant primary con `confidence: 0.83`. Eso le da pistas al participante de cuál es la respuesta esperada en step 3. Spoiler suave de risk event.
  **correction expected:** Cambiar el umbral del followup (p.ej. "confidence > 0.7" o "alta confianza") o eliminar el umbral numérico del prompt del followup.

Manager 30-second test: PASS. Las tres rutas sintéticas (strong/average/risky) producen overrides distintos (pilotar / entrenar / pausar) y la rúbrica + overrides cubren la decisión. La señal está clara: "puede operar agente con gates".

Exercise fit verdict: PASS condicional. La receta sigue el patrón N3 esperado (agent_brief + permission_matrix + run_log_review + decision_memo) y emite evidencia a las 6 dimensiones. Issues: data_table_triage está fuera de catálogo para N3 (P2) y `workflow_builder` declarado pero ausente (P2).

Research freshness verdict: PASS. `freshness.type: current`, `evergreen_weight: 30`, `current_weight: 70`, `last_verified_at: 2026-05-20`, `refresh_due_at: 2026-08-20`. Cumple Gate 2. Recordatorio: revalidar HubSpot/Gmail/Zapier agent capabilities antes de 2026-08-20.

Final route to fix:
1. Codex: agregar `keep` a opciones de step 1 para campos PII y reajustar `target_seconds` o `total_seconds` (P1, bloqueantes para publish).
2. Claude Code (autor): reescribir `assignment_brief` específico al caso; cambiar umbral 0.8 del followup; decidir si `workflow_builder` se agrega como step real o se quita del meta (P2).
3. Catalog owner: decidir si `data_table_triage` se extiende a N3 en `EXERCISE_BLOCK_CATALOG.yaml` (P2).
4. Reviewer humano: re-correr `npm run simulador:review-case` tras P1 cerrados.
