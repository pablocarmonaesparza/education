"use client";

/**
 * /staff/matriz — matriz dimensión × banda del equipo (vista manager).
 * Una sola vista: cuántas personas cayeron en cada banda por dimensión.
 */

import StaffDashboard from "../StaffDashboard";

export default function StaffMatrizPage() {
  return <StaffDashboard section="matriz" />;
}
